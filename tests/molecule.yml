---
ansible:
  playbook: test.yml
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: tower
      ansible_groups:
        - ansible
      image: gahancorpcfo/ubuntu-xenial-ansible
      image_version: latest
      hostname: tower
      port_bindings:
        8800: 8800
    - name: pg 
      ansible_groups:
        - database 
      image: gahancorpcfo/ubuntu-xenial-ansible
      image_version: latest
      hostname: pg 
      port_bindings:
        5432: 5432
verifier:
  name: testinfra
  options:
    junitxml: report.xml
    connection: docker
    hosts: tower
...
# vim: ft=ansible:

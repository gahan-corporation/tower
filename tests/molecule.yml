---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: tower 
      ansible_groups:
        - ansible 
      image: gahancorpcfo/ubuntu-xenial-ansible 
      image_version: latest
      hostname: tower
      playbook: test.yml
verifier:
  name: testinfra
...
# vim: ft=ansible:
